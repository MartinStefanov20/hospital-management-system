apiVersion: v1
kind: Service
metadata:
  name: hms
  labels:
    app: hms
spec:
  type: NodePort
  selector:
    app: hms
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
