# Example only. Create the real secret out-of-band, e.g.:
#   kubectl create secret generic hms-db-credentials \
#     --from-literal=username=postgres \
#     --from-literal=password='<real-password>' \
#     --from-literal=demo-reset-token='<random-token>'
apiVersion: v1
kind: Secret
metadata:
  name: hms-db-credentials
type: Opaque
stringData:
  username: postgres
  password: change-me
  demo-reset-token: change-me
